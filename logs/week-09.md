# Week 9

**Dates:** 08-17 to 08-21

## Goals

- Build a compact neuroevolutionary prototype

## Approach and Implementation

- I will read research papers and try to interpret a neueroevolutionary CNN into my pipeline.

- I have to remember: I am not building a full neuroevolutionary model, just one that is inspired by HyperNEAT.

## Results

- I have built a compact and lightweight neuroevolutionary model. The model takes a config.yaml file like all CNNs before it.

- The model will take in information like epochs and data splits, but also population size and generations.

- The robustness suite also seems to work on the outputted metrics file generated by the training run of the neuroevolutionary model.

- This neuroevolutionary model also seems to be capping out at around 40% accuracy.

## Notes

- While the model seems to be capping out at 40% accuracy, it seems to be much faster than the previous CNNs.

- I still suspect that there is some problem related to how the data is fed into the CNNs. 

- The confusion matrices suggest that the classifications provided by the CNNs tend to be sparse in some areas and concentrated in others.

